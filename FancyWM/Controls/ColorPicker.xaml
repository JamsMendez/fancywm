<UserControl x:Class="FancyWM.Controls.ColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FancyWM.Controls"
             mc:Ignorable="d"
             d:DesignWidth="240">
    <StackPanel Orientation="Vertical" Width="240" Margin="12">
        <Grid x:Name="SvArea" Height="160" Margin="0,0,0,12"
              MouseDown="OnSvMouseDown" MouseMove="OnSvMouseMove" MouseUp="OnSvMouseUp">
            <Rectangle x:Name="SvHueRect" RadiusX="4" RadiusY="4" />
            <Rectangle RadiusX="4" RadiusY="4">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="White" Offset="0" />
                        <GradientStop Color="Transparent" Offset="1" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Rectangle RadiusX="4" RadiusY="4">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="Transparent" Offset="0" />
                        <GradientStop Color="Black" Offset="1" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Border x:Name="SvThumb" Width="12" Height="12" CornerRadius="6"
                    BorderBrush="White" BorderThickness="2" IsHitTestVisible="False"
                    HorizontalAlignment="Left" VerticalAlignment="Top">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.6" />
                </Border.Effect>
            </Border>
        </Grid>

        <Grid x:Name="HueTrack" Height="16" Margin="0,0,0,12"
              MouseDown="OnHueMouseDown" MouseMove="OnHueMouseMove" MouseUp="OnHueMouseUp">
            <Rectangle RadiusX="8" RadiusY="8">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#FFFF0000" Offset="0" />
                        <GradientStop Color="#FFFFFF00" Offset="0.167" />
                        <GradientStop Color="#FF00FF00" Offset="0.333" />
                        <GradientStop Color="#FF00FFFF" Offset="0.5" />
                        <GradientStop Color="#FF0000FF" Offset="0.667" />
                        <GradientStop Color="#FFFF00FF" Offset="0.833" />
                        <GradientStop Color="#FFFF0000" Offset="1" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Border x:Name="HueThumb" Width="4" Background="White" IsHitTestVisible="False" HorizontalAlignment="Left">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.6" />
                </Border.Effect>
            </Border>
        </Grid>

        <Grid x:Name="AlphaTrack" Height="16" Margin="0,0,0,16"
              MouseDown="OnAlphaMouseDown" MouseMove="OnAlphaMouseMove" MouseUp="OnAlphaMouseUp">
            <Rectangle Fill="{StaticResource CheckerboardBrush}" RadiusX="8" RadiusY="8" />
            <Rectangle RadiusX="8" RadiusY="8">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop x:Name="AlphaStopTransparent" Offset="0" />
                        <GradientStop x:Name="AlphaStopOpaque" Offset="1" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Border x:Name="AlphaThumb" Width="4" Background="White" IsHitTestVisible="False" HorizontalAlignment="Left">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.6" />
                </Border.Effect>
            </Border>
        </Grid>

        <DockPanel LastChildFill="True">
            <Border DockPanel.Dock="Right" Width="32" Height="32" Margin="8,0,0,0"
                    BorderBrush="{DynamicResource SystemControlBackgroundBaseHighBrush}" BorderThickness="1">
                <Grid>
                    <Rectangle Fill="{StaticResource CheckerboardBrush}" />
                    <Rectangle>
                        <Rectangle.Fill>
                            <SolidColorBrush x:Name="PreviewBrush" />
                        </Rectangle.Fill>
                    </Rectangle>
                </Grid>
            </Border>
            <TextBox x:Name="HexTextBox" VerticalAlignment="Center"
                     KeyDown="OnHexTextBoxKeyDown" LostFocus="OnHexTextBoxLostFocus" />
        </DockPanel>
    </StackPanel>
</UserControl>
